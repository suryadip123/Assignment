<%= form_for(@newspost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new Post/ News..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>