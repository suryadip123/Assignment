<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h3><%= current_user.name %></h3>
<span><%= link_to "view my profile", root_path %></span>
<span><%= pluralize(current_user.newsposts.count, "Post") %></span>